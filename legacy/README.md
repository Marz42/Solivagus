# Legacy MVP baselines

Frozen single-script translator used as the Solivagus Phase 0 characterization baseline.

| File | Role |
|------|------|
| `pdf_translate_cli_v0_2.py` | Canonical archived MVP (`SCRIPT_VERSION` includes hotfix lineage) |

Design notes and older copies may still exist under `project-brief/` for human reading. **Tests and Phase 0 acceptance must import from `legacy/`.**

Do not extend this script with new product features. Port behavior into `src/solivagus/` during later phases.
